# Testing dependencies for qsource3-mqtt

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Test utilities
coverage>=7.0.0
mock>=4.0.0

# Required for running the application (if not already installed)
paho-mqtt>=1.6.0
pyvisa>=1.13.0
PyYAML>=6.0